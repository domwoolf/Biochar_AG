% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{run_scenario}
\alias{run_scenario}
\title{Run Scenario Spatial TEA}
\usage{
run_scenario(template, layers, params, vec = NULL)
}
\arguments{
\item{template}{Reference SpatRaster template.}

\item{layers}{List of spatial layers (SpatRaster objects).}

\item{params}{Scenario parameter list.}

\item{vec}{Optional list of pre-extracted 1D spatial vectors from \code{load_region_data()$vec}.}
}
\value{
A list containing \code{net} (SpatRaster stack), \code{abate} (SpatRaster stack), \code{opt} (SpatRaster), and optionally \code{vec_res}.
}
\description{
Evaluates spatial TEA across BES, BECCS, and BEBCS for a scenario.
If \code{vec} (pre-extracted 1D spatial vectors) is provided, executes fast vectorized
calculations and maps the results onto SpatRaster objects matching \code{template}.
Otherwise, falls back to standard raster-based \code{run_spatial_tea}.
}
