% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_tea.R
\name{run_spatial_tea}
\alias{run_spatial_tea}
\title{Run Spatial TEA Analysis}
\usage{
run_spatial_tea(
  template_raster,
  params,
  spatial_layers = list(),
  fun = calculate_beccs,
  collection_radius_km = 50
)
}
\arguments{
\item{template_raster}{A \code{SpatRaster} (terra) defining the extent and resolution.}

\item{params}{A list of baseline parameters.}

\item{spatial_layers}{A named list of \code{SpatRaster} objects for spatially-varying parameters.
Likely candidates: \code{soil_temp} (for Fperm), \code{biomass_density} (Mg/km2).}

\item{fun}{The TEA function to run (default: \code{calculate_beccs}).}

\item{collection_radius_km}{Radius of biomass collection zone to calculate plant scale
(default: 50 km). Used with \code{biomass_density} to determine \code{plant_mw}.}
}
\value{
A \code{SpatRaster} with layers for key outputs (NPV, Total Cost, Abatement, etc.).
}
\description{
Runs the Techno-Economic Assessment over a spatial grid defined by a template raster.
Calculates locally-specific metrics such as CO2 transport distance and plant scale
(based on biomass density).
}
