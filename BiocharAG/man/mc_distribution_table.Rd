% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_sampling.R
\name{mc_distribution_table}
\alias{mc_distribution_table}
\title{Build Monte Carlo Marginal Distributions}
\usage{
mc_distribution_table(params_df, central = list(), names = NULL)
}
\arguments{
\item{params_df}{Parameter table (as read from \code{parameters.csv}).}

\item{central}{Named list of central values (e.g. \code{set_scenario(region = r)}); parameters
missing from it fall back to \code{default_value}.}

\item{names}{Optional subset of parameter names to include.}
}
\value{
A data frame with columns \code{name}, \code{distribution}, \code{min}, \code{mode}, \code{max}.
}
\description{
Resolves the sampling bounds in \code{parameters.csv} against central (typically regional)
values. \code{dist_bounds = "relative"} bounds are multipliers on the central value, so each
region gets its own distribution; \code{"absolute"} bounds are used as given.
}
